import { styled } from 'styled-components';

const style = {
  Login: styled.div`
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  `,
};

export default style;
